# Кредитные продукты (Credit Products)

Документ описывает функцию кредитных продуктов, которая позволяет пользователям отслеживать и управлять своими долгами и кредитными обязательствами, включая кредитные карты, личные займы и рассрочки.

## Цель и ценность

- Пользователь может создавать кредитные продукты (кредитная карта, займ, рассрочка) для отслеживания баланса и платежей.
- Автоматическое начисление процентов на основе APR (годовая процентная ставка).
- Автоматическое уменьшение баланса долга при записи платежа по кредиту.
- Единая категория "Credit" для платежей по кредитам.
- Пользователь видит, сколько он должен, сколько процентов накапливается, и как платежи уменьшают долг со временем.

## UX и пользовательский поток

### Добавление транзакции типа Credit

1. Пользователь нажимает плавающую кнопку "+"
2. Выбирает тип транзакции "Credit" (💳)
3. Вводит сумму платежа
4. Выбирает кредитный продукт из списка активных продуктов
5. Подтверждает транзакцию
6. Система автоматически применяет платеж к выбранному кредитному продукту

### Создание кредитного продукта

**Поля:**
- Name (обязательно) - название кредитного продукта
- Initial Debt Amount (principal) - начальная сумма долга
- APR (%) (обязательно) - годовая процентная ставка
- Credit Type - тип кредита:
  - Credit Card (кредитная карта)
  - Fixed Loan (фиксированный займ)
  - Installment Plan (рассрочка/BNPL)
- Monthly Minimum Payment (опционально) - минимальный ежемесячный платеж
- Payment Due Date (опционально) - день месяца для платежа (1-31)
- Loan Term (months) (опционально) - срок займа в месяцах (для фиксированных займов)
- Notes (опционально) - заметки

**Автоматические расчеты:**
- Daily interest rate = APR / 100 / 365
- Monthly estimated interest
- Remaining balance (изначально = principal)

## Данные и модели

### CreditProduct

```typescript
interface CreditProduct {
  id: string;
  name: string;
  principal: number;              // Начальная сумма долга
  remainingBalance: number;        // Текущий остаток долга
  apr: number;                     // Годовая процентная ставка (%)
  dailyInterestRate: number;       // Дневная ставка (apr / 100 / 365)
  creditType: 'credit_card' | 'fixed_loan' | 'installment';
  loanTermMonths?: number;         // Срок займа в месяцах (для амортизации)
  monthlyMinimumPayment?: number;  // Минимальный ежемесячный платеж
  dueDate?: number;                // День месяца (1-31)
  accruedInterest: number;         // Накопленные проценты
  totalPaid: number;               // Общая сумма выплаченных платежей
  status: 'active' | 'paid_off' | 'overdue';
  startDate: string;               // ISO string
  createdAt: string;               // ISO string
  updatedAt: string;               // ISO string
  note?: string;                   // Опциональные заметки
}
```

### Transaction расширение

```typescript
interface Transaction {
  id: string;
  type: 'expense' | 'income' | 'saved' | 'credit';
  amount: number;
  category?: string;
  creditProductId?: string;  // Ссылка на кредитный продукт для кредитных транзакций
  createdAt: string;
}
```

## Хранилище и стейт

### AsyncStorage

- Новый ключ `creditProducts` для массива `CreditProduct[]`

### Стейт-слой

- `CreditProductsProvider` (по аналогии с MiniBudgetsProvider) с методами:
  - `createCreditProduct` - создание кредитного продукта
  - `updateCreditProduct` - обновление кредитного продукта
  - `deleteCreditProduct` - удаление кредитного продукта
  - `applyPayment` - применение платежа к кредитному продукту
  - `getActiveCreditProducts` - получение активных кредитных продуктов
  - `getCreditProductById` - получение продукта по ID

### Интеграция с TransactionsProvider

- При создании транзакции типа `credit` с `creditProductId`:
  - Транзакция сохраняется как `type: 'credit'`, `category: 'Credit'`
  - Автоматически вызывается `applyPayment(productId, amount)` для применения платежа

## Бизнес-правила

### Расчет процентов

#### Кредитные карты (Revolving Credit)

**Ежедневное начисление процентов:**
```
dailyInterestRate = APR / 100 / 365
dailyInterest = remainingBalance * dailyInterestRate
accruedInterest += dailyInterest
```

**Применение платежа:**
- Приоритет: сначала проценты, затем основной долг
- Если платеж >= накопленных процентов:
  - Погашаются все проценты
  - Остаток идет на основной долг
- Если платеж < накопленных процентов:
  - Уменьшаются только проценты

#### Фиксированные займы (Amortized Loans)

**Месячная процентная ставка:**
```
monthlyRate = APR / 100 / 12
```

**Формула ежемесячного платежа (амортизация):**
```
payment = principal * monthlyRate / (1 - (1 + monthlyRate)^(-loanTermMonths))
```

**Разделение платежа:**
- Проценты: `interestPortion = remainingBalance * monthlyRate`
- Основной долг: `principalPortion = payment - interestPortion`
- Остаток: `remainingBalance -= principalPortion`

#### Рассрочки (Installment Plans)

**Прямолинейное уменьшение:**
```
monthlyPayment = principal / numberOfMonths
remainingBalance -= paymentAmount
```

Если APR > 0, используется формула амортизации для фиксированных займов.

### Статусы кредитных продуктов

- `active` - активный кредит
- `paid_off` - полностью погашен
- `overdue` - просрочен (если платеж не внесен до dueDate)

### Обновление статуса

- При платеже, если `remainingBalance === 0` и `accruedInterest === 0` → статус = `paid_off`
- Если платеж < `monthlyMinimumPayment` → статус = `below_minimum` (будущее расширение)
- Если прошла дата платежа без оплаты → статус = `overdue` (будущее расширение)

## Изменения в существующих функциях

### Add Transaction Flow

- Добавлен тип транзакции "Credit" в список выбора типов
- При выборе типа "Credit":
  - Категория автоматически устанавливается в "Credit"
  - На шаге выбора категории показывается список активных кредитных продуктов
  - При сохранении транзакции автоматически применяется платеж к выбранному продукту

### CategorySelectionStep

- Для типа `credit` показывается список кредитных продуктов вместо категорий
- Каждый продукт отображается с:
  - Названием
  - Текущим остатком долга
  - Иконкой 💳

### ConfirmStep

- Для кредитных транзакций дополнительно отображается название кредитного продукта

## API слоя данных (локальный контракт)

### CreditProductsProvider

```typescript
// Создание кредитного продукта
createCreditProduct(input: {
  name: string;
  principal: number;
  apr: number;
  creditType: CreditProductType;
  loanTermMonths?: number;
  monthlyMinimumPayment?: number;
  dueDate?: number;
  note?: string;
}): Promise<CreditProduct>

// Обновление кредитного продукта
updateCreditProduct(id: string, input: {
  name?: string;
  principal?: number;
  apr?: number;
  creditType?: CreditProductType;
  loanTermMonths?: number;
  monthlyMinimumPayment?: number;
  dueDate?: number;
  note?: string;
}): Promise<void>

// Удаление кредитного продукта
deleteCreditProduct(id: string): Promise<void>

// Применение платежа
applyPayment(productId: string, amount: number): Promise<void>

// Получение активных продуктов
getActiveCreditProducts(): CreditProduct[]

// Получение продукта по ID
getCreditProductById(id: string): CreditProduct | null
```

## Аналитика

События для отслеживания (будущее расширение):

- `credit_product_created {name, principal, creditType, apr}`
- `credit_product_edited {productId, fieldsChanged}`
- `credit_product_deleted {productId}`
- `credit_product_payment_made {productId, amount}`
- `credit_product_overdue {productId}`
- `credit_product_paid_off {productId}`
- `credit_transaction_added {productId, amount}`

## План внедрения (v1)

1. ✅ Модели и типы: CreditProduct, обновление Transaction
2. ✅ Хранилище: AsyncStorage ключ, функции загрузки/сохранения
3. ✅ Провайдер: CreditProductsProvider с базовыми методами
4. ✅ Интеграция транзакций: поддержка creditProductId в TransactionsProvider
5. ✅ UI:
   - Добавлен тип "Credit" в AddTransactionModal
   - Обновлен CategorySelectionStep для выбора кредитных продуктов
   - Обновлен ConfirmStep для отображения информации о продукте
6. ⏳ Автоматическое применение платежей при сохранении транзакции
7. ⏳ UI для создания/редактирования кредитных продуктов (будущее расширение)
8. ⏳ Dashboard кредитных продуктов (будущее расширение)
9. ⏳ Начисление ежедневных процентов (будущее расширение)
10. ⏳ Уведомления о минимальных платежах (будущее расширение)

## Открытые вопросы

1. Когда реализовать UI для создания кредитных продуктов? (сейчас можно создавать только программно)
2. Нужен ли отдельный экран для управления кредитными продуктами?
3. Как часто начислять проценты? (ежедневно при запуске приложения или по требованию)
4. Нужны ли уведомления о минимальных платежах в v1?
5. Как обрабатывать редактирование/удаление транзакций кредита? (откат платежа)

## Версия

**Дата:** 2025-01-XX  
**Версия:** 1.0.0 (базовая реализация)  
**Статус:** В разработке

## Изменения

**[2025-01-XX]** - Добавлена базовая поддержка кредитных продуктов:
- Тип транзакции "Credit"
- CreditProductsProvider для управления продуктами
- Интеграция с AddTransaction flow
- Автоматическое применение платежей

