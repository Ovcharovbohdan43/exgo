# Как сбросить онбординг и показать экран приветствия снова

Если вы уже прошли онбординг и хотите увидеть его снова, есть несколько способов:

## Способ 1: Кнопка на HomeScreen (Самый простой)

1. Откройте приложение
2. На главном экране найдите красную кнопку **"🔄 Reset Onboarding (Dev)"**
3. Нажмите на неё
4. Подтвердите сброс
5. **Перезапустите приложение** (закройте и откройте снова)

## Способ 2: Через консоль разработчика (Expo)

Если вы используете Expo Go или Expo Dev Client:

1. Откройте консоль Metro (где запущен `npm start`)
2. Нажмите `j` чтобы открыть Dev Menu
3. Или встряхните устройство и выберите "Debug Remote JS"
4. В консоли браузера выполните:

```javascript
// Очистить все данные
import AsyncStorage from '@react-native-async-storage/async-storage';
await AsyncStorage.multiRemove(['settings', 'transactions']);
console.log('✅ Данные очищены! Перезапустите приложение.');
```

## Способ 3: Через React Native Debugger

1. Откройте React Native Debugger
2. В консоли выполните:

```javascript
const AsyncStorage = require('@react-native-async-storage/async-storage').default;
AsyncStorage.multiRemove(['settings', 'transactions']).then(() => {
  console.log('✅ Данные очищены! Перезапустите приложение.');
});
```

## Способ 4: Очистить кеш Expo и переустановить

```bash
# Остановите Metro bundler (Ctrl+C)
# Очистите кеш
npx expo start -c

# Или полностью очистите
rm -rf node_modules
npm install
npx expo start -c
```

**Важно:** После очистки данных обязательно **перезапустите приложение** (закройте и откройте снова), чтобы изменения вступили в силу.

## Способ 5: Удалить и переустановить приложение

На устройстве:
- iOS: Долгое нажатие на иконку → Удалить → Переустановить через Expo Go
- Android: Настройки → Приложения → ExGo → Удалить → Переустановить

---

**Примечание:** Кнопка "Reset Onboarding (Dev)" на HomeScreen предназначена только для разработки. В production версии её следует удалить.

